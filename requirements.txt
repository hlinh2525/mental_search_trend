numpy>=1.24
pandas>=2.0

scipy>=1.10
statsmodels>=0.14

matplotlib>=3.7

jupyter>=1.0
notebook>=7.0
nbconvert>=7.0
ipykernel>=6.0

tabulate>=0.9

openpyxl>=3.1

# Standard-library modules used in the notebooks:
# pathlib.Path  -> built into Python; do not install with pip
# itertools     -> built into Python; do not install with pip
